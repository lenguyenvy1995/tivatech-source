<a href="{{ route('admin.roles.edit', $role->id) }}" class="btn btn-sm btn-primary">Sửa</a>
<a href="#" class="btn btn-sm btn-danger btn-delete" data-url="{{ route('admin.roles.destroy', $role->id) }}">Xóa</a>
